{% extends 'base.html.twig' %}

{% block title %}User{% endblock %}

{% block body %}
    <h1>Mon compte</h1>
    <div class="row">
        <div class="col-md-4">
            <img src="{{asset('images/avatars/')}}{{user.avatar}}" class="rounded-circle shadow-4"
                  style="width: 350px;" alt="Avatar" />
                  <h5>{{user.pseudo}}</h5>
        </div>

        <div class="col-md-8">

            <table class="table">
                <tbody>
                    {# <tr>
                        <th>Id</th>
                        <td>{{ user.id }}</td>
                    </tr> #}
                    <tr>
                        <th>Email</th>
                        <td>{{ user.email }}</td>
                    </tr>
                    <tr>
                        <th>Roles</th>
                        <td>{{ user.roles ? user.roles|json_encode : '' }}</td>
                    </tr>
                    {# <tr>
                        <th>Password</th>
                        <td>{{ user.password }}</td>
                    </tr>
                    <tr>
                        <th>Avatar</th>
                        <td>{{ user.avatar }}</td>
                    </tr>
                    <tr>
                        <th>Pseudo</th>
                        <td>{{ user.pseudo }}</td>
                    </tr> #}
                </tbody>
            </table>
        </div>
    </div>



    {# <a href="/profile">back to list</a> #}

    <a href="{{ path('app_profile_edit', {'id': user.id}) }}">edit</a>

    {{ include('profile/_delete_form.html.twig') }}
{% endblock %}